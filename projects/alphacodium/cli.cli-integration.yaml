# CLI Integration Analysis: AlphaCodium
# Spec: specs/cli-integration.spec.yaml

project: "alphacodium"
cli_integration_detected: false

summary: "AlphaCodium does NOT invoke the Claude Code CLI binary. Uses LiteLLM abstraction layer instead."

invocations: []

flags_summary: {}
