# CLI Integration Analysis: gptme
# Spec: specs/cli-integration.spec.yaml

project: "gptme"
cli_integration_detected: false

summary: "gptme does NOT invoke the Claude Code CLI binary. Uses direct Anthropic SDK instead."

invocations: []

flags_summary: {}
